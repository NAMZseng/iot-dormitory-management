<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.nam.dormitorymanage.backend.dao.StudentInfoDao">

    <select id="getStudents" resultType="StudentInfo">
        SELECT num, name, school, major, teacher_name
            ,teacher_tel, building_num, room_num
        FROM student_info
        WHERE building_num = #{buildingNum}
    </select>

    <select id="list" resultType="StudentInfo">
        SELECT num, name, school, major, teacher_name
            ,teacher_tel, building_num, room_num
        FROM student_info
    </select>

    <insert id="add" parameterType="StudentInfo">
        INSERT INTO student_info(num, name, school, major, teacher_name
                            ,teacher_tel, building_num, room_num)
        VALUES(#{num},#{name},#{school},#{major},#{teacherName},
                #{teacherTel},#{buildingNum},#{roomNum})
    </insert>

    <delete id="delete">
        DELETE FROM student_info
        WHERE  num = #{num}
    </delete>
</mapper>