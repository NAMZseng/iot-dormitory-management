<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.nam.dormitorymanage.backend.dao.HumitureInfoDao">

    <insert id="addData">
        insert into humiture_info(mac_address, temperature, humidity)
        values(#{macAddress}, #{temperature}, #{humidity})
    </insert>

    <select id="getTodayData" resultType="HumitureInfo">
        select
            a.mac_address,
            building_num,
            location,
            collect_time,
            temperature,
            humidity
        from sensors_info a inner join humiture_info b
        on a.mac_address=b.mac_address
        where a.building_num = #{buildingNum} and
              collect_time >= #{dawnTime}
        order by collect_time desc
    </select>

</mapper>