<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.nam.dormitorymanage.backend.dao.StaffInfoDao">

    <select id="getStaffInfo" resultType="StaffInfo">
        select num, name, tel, building_num, title, password
        from staff_info
        where tel = #{tel} and password = #{password}
    </select>

    <update id="upadatePassord">
        update staff_info
        set password = #{newPassword}
        where tel = #{tel} and password = #{oldPassword}
    </update>

    <select id="fingTel" resultType="String">
        select tel from staff_info
        where
        tel = #{tel}
    </select>

    <update id="updateTel">
        update staff_info
        set tel = #{newTel}
        where tel = #{oldTel} and password = #{password}
    </update>

</mapper>